{% extends 'base.html.twig' %}

{% block title %}Ajouter un soft{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Ajout d'un soft</h4>
            </div>
        </div>

           {{ form_start(form)}}  


        {{ form_label(form.LibelleSoft)}}
        {{ form_widget(form.LibelleSoft)}}
        {{ form_label(form.Ajouter)}}
        {{ form_widget(form.Ajouter)}}


        {{ form_end(form)}}
    </div>
<div>

<a href='/Admin'>Retour</a>
</div>
{% endblock %}
